public class RecyclingCentre{
    //list of container objects. Contains both a name and an ID to compare the trashID to
    Container metalContainer = new Container("Metal container", 1111);
    Container plasticContainer = new Container( "Plastic container", 2222);
    Container ResidualWasteContainer = new Container( "Residual waste container", 3333);
    Container DangerousWasteContainer = new Container( "Dangerous waste container", 4444);
}
